# Core runtime dependencies
playwright==1.51.0
supabase==2.5.0
requests==2.32.3
google-generativeai==0.8.5
fastapi==0.115.8
uvicorn[standard]==0.34.0

# Optional: load .env when running locally (not needed in CI)
python-dotenv==1.0.1
